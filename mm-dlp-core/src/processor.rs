//! Audio processor module.

pub struct AudioProcessor;

impl AudioProcessor {
    pub fn new() -> Self {
        Self
    }
}

impl Default for AudioProcessor {
    fn default() -> Self {
        Self::new()
    }
}
